name: event_bus_auto_codegen
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com
author: Tshine <gtshine@gmail.com>

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  build_runner: ^1.10.0
  source_gen: ^0.9.5
  event_bus_auto:
    path: ../event_bus_auto

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
