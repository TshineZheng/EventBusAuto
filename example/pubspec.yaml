name: example
description: A starting point for Dart libraries or applications.
version: 1.0.0
homepage: https://github.com/TshineZheng/EventBusAuto
author: Tshine <gtshine@gmail.com>

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  event_bus: ^1.1.1
  event_bus_auto: ^0.0.1

dependencies_overrides:
  event_bus_auto:
    path: ../event_bus_auto

dev_dependencies:
  build_runner: ^1.10.0
  event_bus_auto_codegen:
    path: ../event_bus_auto_codegen

dev_dependency_overrides:
  event_bus_auto:
    path: ../event_bus_auto
  event_bus_auto_codegen:
    path: ../event_bus_auto_codegen